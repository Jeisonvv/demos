<!-- # demos
0. hay que instalar typeScript para esto utilizamos el siguiete codigo en la terminal (npm install --save-dev typescript ts-node) de esta forma queda en el proyecto solamente 
1. el proyecto de typescript simpre hay que configurar el proyecto para esto utilizamos el comando tsc  --init esto nos crea un archivo (tsconfig.ts).
2. despues de esto buscamos en el archivo la propiedad (outdir) y ponemos en la carpeta donde se va acrear el archivo js 
3. por ultimo para que no cree varios archivos en el arrchivo (tsconfig.ts) creamos una propiedad ("files":[ index.ts ]) esto para que lea solo el index.tx.
4. para que se actualice el archivo creamos un comando run build con la propiedad tcs en el rachivo package.js -->

